# Nix
result
result-*

# Direnv
.direnv

# Local / machine-specific
secrets/*.env
!secrets/*.env.example
.DS_Store
*.swp
defaults.txt
Brewfile

# Third-party reference tree (nested template; contains sops secrets layout)
example-config/
